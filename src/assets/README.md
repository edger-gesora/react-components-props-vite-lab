# React Personal Blog Project

A simple personal blog web application built with React to display articles, about information, and a header.

## Features
- Dynamic header with blog name.
- Sidebar with profile image and about description.
- Article list rendering multiple blog posts using reusable components.

## How to Run the Project

1. Clone this repository to your local machine.
2. Install the required dependencies by running:
   ```bash
   npm install